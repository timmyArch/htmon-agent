require 'spec_helper'

describe Htmon::Agent do
  it 'has a version number' do
    expect(Htmon::Agent::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
